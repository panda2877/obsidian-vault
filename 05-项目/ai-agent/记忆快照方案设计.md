# 记忆快照方案设计

> 状态：草稿
> 负责人：如音（技术调研）
> 需求来源：老大
> 创建时间：2026-05-02
> 关联：Hermes Agent v0.8+ 记忆系统

---

## 一、问题定义

### 1.1 痛点

| 场景 | 问题 |
|------|------|
| **Session 结束无法快速恢复上下文** | 会话结束后，进程退出，上下文全部丢失。下次新会话启动时没有任何残留记忆，只能靠 session_search 手动检索 |
| **任务转交时无法连带记忆** | delegate_task 转交给 subagent 时，subagent 有独立进程和 memory provider，但没有任何来自父 agent 的上下文 |

### 1.2 现有系统分析

**现有 memory provider（语义记忆）：**

- mem0 / Zep / Honcho 等：存储经 LLM 提取后的 facts/entities
- MemOS（memos 插件）：独立记忆 OS，有 Hermes 集成，存的是提取后的事实，不是原始会话快照
- **共同缺陷**：都是语义压缩后的信息，不是原始会话快照，有信息损失

**Hermes 内置记忆：**

- MEMORY.md / USER.md：2200/1375 字符限制，冻结快照机制，按需加载
- `session_search`：可搜索历史，但不会自动注入上下文，需模型主动调用

**结论：** 现有系统无法满足"原始会话快照"需求，需要新增机制。

---

## 二、核心设计原则

1. **任务驱动**：快照内容以"任务相关信息"为核心，而非完整对话日志
2. **原始快照优先**：保存原始会话上下文，不做语义压缩，确保信息不丢失
3. **所有 agent 共享 provider**：每个独立 agent（独立网关 + workspace）都运行自己的 memory provider 实例
4. **文件持久化**：快照写入文件系统，不依赖内存

---

## 三、方案一：MemoryProvider 插件（完整实现）

### 3.1 架构可行性 ✓

**源码确认事实：**

- `plugins/memory/__init__.py` 的 `load_memory_provider(name)` 支持从 `plugins/memory/<name>/`（打包）和 `$HERMES_HOME/plugins/<name>/`（用户）加载
- `run_agent.py`（L1442-1486）读取 `memory.provider` 配置项，激活对应 provider
- **内置 BuiltinMemoryProvider 永远存在**，外部 provider与其共存，不冲突

**Plugin 目录结构：**

```
$HERMES_HOME/plugins/memory/snapshot/
├── __init__.py          # SnapshotMemoryProvider 类
└── plugin.yaml          # name, version, hooks
```

**plugin.yaml 示例：**

```yaml
name: snapshot
version: 1.0.0
description: "会话快照 provider — 写入/读取原始会话上下文"
hooks:
  - on_session_end
pip_dependencies: []
```

### 3.2 `on_session_end` 钩子（写入快照）✓

**接口确认：**

```python
# memory_provider.py L153
def on_session_end(self, messages: List[Dict[str, Any]]) -> None:
    """Called when a session ends (CLI exit, /reset, gateway expiry).
    NOT called per-turn — only at actual session boundaries."""
```

**触发时机（已确认）：**
- CLI exit（`/exit`、`Ctrl+C`）
- `/reset`
- Gateway session 驱逐
- 上下文压缩导致 session_id 轮换（`commit_memory_session`）
- AIAgent 析构（`shutdown_memory_provider`）

**messages 参数结构：** 完整会话历史，`[{role, content, ...}, ...]`，原材料齐全。

**写入逻辑：**

```python
def on_session_end(self, messages: List[Dict[str, Any]]) -> None:
    # 提取任务相关信息（去重+截断）
    snapshot_data = {
        "session_id": self._session_id,
        "timestamp": time.time(),
        "messages": messages,
        "task_context": self._extract_task_context(messages),
    }
    snapshot_path = self._get_snapshot_path()
    with open(snapshot_path, "w") as f:
        json.dump(snapshot_data, f, ensure_ascii=False)
```

### 3.3 快照读取机制（新 session 恢复）⚠

**架构盲点：** `on_session_end` 只负责写入，没有配套的"session 启动时自动读取"原生钩子。

**解法：** 在 `system_prompt_block()` 或 `prefetch()` 里做加载判断。

```python
def system_prompt_block(self) -> str:
    snapshot = self._load_latest_snapshot()
    if snapshot:
        return (
            f"<previous-session-snapshot>\n"
            f"[Session: {snapshot['session_id']} | "
            f"Time: {datetime.fromtimestamp(snapshot['timestamp']).isoformat()}]\n"
            f"{snapshot.get('task_context', '')}\n"
            f"</previous-session-snapshot>"
        )
    return ""
```

**文件名约定：** 使用固定路径（如 `~/.hermes/profiles/<profile>/memory/snapshot_prev.json`），不依赖 session_id 映射。

### 3.4 任务转交（delegate_task）— 所有 agent 共享 provider ✓

**关键发现：** `delegate_task` 的 `context` 参数会被嵌入子 agent 的系统提示词：

```python
# delegate_tool.py L90-110
def _build_child_system_prompt(goal, context=None, ...):
    parts = [
        f"YOUR TASK:\n{goal}",
    ]
    if context and context.strip():
        parts.append(f"\nCONTEXT:\n{context}")  # ← 快照注入点
    ...
    return "\n".join(parts)

# delegate_tool.py L390 — 子 agent 接收
ephemeral_system_prompt=child_prompt,  # 包含 context
skip_memory=True,                      # 子 agent 不走独立 memory provider
```

**任务转交流程（修订后）：**

```
父 agent                        子 agent
   |                                |
   |-- delegate_task(goal,         |
   |            context=<快照>) ---->|  ← context 参数携带快照内容
   |   (ephemeral_system_prompt)    |     子 agent 在系统提示词中收到完整上下文
   |                                |
   |<-- result ----------------------|
   |
   +-- on_delegation(task, result)  ← 父 agent 记录委托结果
```

**关键约束：**

- 子 agent 的 `skip_memory=True` 表示**子 agent 自身不初始化 memory provider**（无独立记忆写入）
- 但子 agent **能看到 `ephemeral_system_prompt` 中的 context**（包含快照）
- 父 agent 和子 agent 各自有**独立的 provider 实例**，共享的是 context 注入机制而非同一份内存状态

**快照传递方式：**

在父 agent 调用 `delegate_task` 时，通过 `context` 参数注入：

```python
# 父 agent 侧
snapshot = self._load_latest_snapshot()
delegate_task(
    goal="继续完成XXX任务",
    context=f"[上节会话快照]\n{snapshot['task_context']}\n\n[委托任务的具体要求]"
)
```

子 agent 通过 `ephemeral_system_prompt`（即 `child_prompt`）自动收到。

### 3.5 `on_delegation` 钩子（委托结果记录）

```python
def on_delegation(self, task: str, result: str,
                  child_session_id: str = "", **kwargs) -> None:
    """父 agent 侧调用 — 记录委托任务和结果到快照"""
    self._append_delegation_record(task, result, child_session_id)
```

用于父 agent 记录"我曾经委托过这个任务，结果是什么"，形成委托链记忆。

---

## 四、方案二：轻量实现（skill + 快照文件）

如果插件方案成本过高，可以考虑轻量路径：

**核心思路：** 不做 MemoryProvider 插件，写一个工具函数 + 一个启动 skill

| 能力 | 实现方式 |
|------|----------|
| 快照写入 | 在 `~/.hermes/profiles/<name>/skills/` 里写一个 skill，模型在 session 结束时自动调用写入 |
| 快照恢复 | 新 session 启动时，skill 自动读取上一个快照并注入上下文 |
| 任务转交 | 委托前调用 skill 把快照写入文件，子 agent 启动参数传入文件路径 |

**优点：** 不需要理解 plugin 架构，任何人都能维护
**缺点：** 需要模型主动配合调用 skill，无法利用 `on_session_end` 原生钩子

---

## 五、方案对比

| 维度 | 插件方案 | 轻量方案 |
|------|----------|----------|
| 接入方式 | `memory.provider: snapshot` | skill 调用 |
| 快照写入时机 | `on_session_end` 自动 | 模型主动调用 skill |
| 新 session 恢复 | `system_prompt_block()` 自动注入 | skill 自动加载 |
| 任务转交 | `context` 参数注入 | 文件路径传递 |
| 维护成本 | 高（plugin 接口） | 低（纯 skill） |
| 与内置记忆共存 | ✓ | ✓ |
| 推荐度 | ⭐⭐⭐ 首选 | ⭐⭐ 备选 |

---

## 六、实现计划（插件方案）

### Phase 1：核心快照 Provider
- [ ] 实现 `SnapshotMemoryProvider` 类
  - `is_available()` — 始终返回 True
  - `initialize()` — 建立会话，读取上一快照路径
  - `on_session_end(messages)` — 写入快照
  - `system_prompt_block()` — 注入上节快照
  - `on_delegation()` — 记录委托链
- [ ] 创建 `plugin.yaml`
- [ ] 激活：`memory.provider: snapshot`

### Phase 2：快照内容优化
- [ ] 设计任务相关信息的提取逻辑（去噪声、保留关键上下文）
- [ ] 实现快照文件的轮转/过期策略

### Phase 3：任务转交增强
- [ ] 在 `delegate_task` 调用处，检测当前是否有活跃快照
- [ ] 自动将快照内容注入 `context` 参数

---

## 六、独立 Workspace 多 Agent 场景下的额外设计规则

> 以下规则在独立 workspace 多 agent 架构下**必须遵守**，确保快照文件可跨 agent 访问和传递。

### 6.1 快照文件路径隔离（最关键）

每个 agent 独立进程、独立初始化 provider，快照路径必须基于**确定性标识**生成，不能用随机 token。

```python
def _get_snapshot_path(self) -> Path:
    # ❌ 错误：只用 session_id，每次新建都不同，跨 agent 无法推导
    return self.hermes_home / "memory" / f"snapshot_{session_id}.json"
    
    # ✓ 正确：基于 agent_identity + workspace_name
    # 这两个在 delegate_task 委托场景下是可预知的
    return (
        self.hermes_home 
        / "memory" 
        / "snapshot"
        / f"{self.agent_identity}_{self.workspace_name}.json"
    )
```

**为什么这样设计：**
- `agent_identity`：profile 名称，区分不同 agent 实例
- `workspace_name`：工作区标识，delegate_task 时通过 `_resolve_workspace_hint` 传递
- 这两个在父→子委托时是已知量，子 agent 才能找到父的快照

### 6.2 `initialize()` 必须接收 workspace 相关 kwargs

```python
def initialize(self, session_id: str, **kwargs) -> None:
    super().initialize(session_id, **kwargs)
    
    # ✓ 关键：从 kwargs 提取 workspace 上下文
    self.agent_identity = kwargs.get("agent_identity", "default")
    self.workspace_name = kwargs.get("agent_workspace", "hermes")
    self.parent_session_id = kwargs.get("parent_session_id", "")
```

**传入的 kwargs 来源（已确认）：**

| 参数 | 来源 | 用途 |
|------|------|------|
| `agent_identity` | `get_active_profile_name()` | agent 实例标识 |
| `agent_workspace` | 固定值 `"hermes"` | workspace 名称 |
| `parent_session_id` | `delegate_tool.py` 传入 | 父 agent session_id |
| `hermes_home` | `get_hermes_home()` | 存储根路径 |
| `session_id` | 当前 session | 唯一会话标识 |

### 6.3 任务转交时，父→子快照路径必须是子可推算的

delegate_task 的 `context` 参数携带快照内容时，快照路径规则必须让子 agent 能通过 `agent_identity + workspace_name` 推导出来：

```python
# 父 agent 写入快照时
snapshot_name = f"{self.agent_identity}_{self.workspace_name}.json"

# 子 agent 启动时，从 context 或 workspace hint 拿到信息
# 通过同样的命名规则反推父快照路径
def _get_parent_snapshot_path(workspace_name: str, parent_identity: str) -> Path:
    return (
        self.hermes_home / "memory" / "snapshot"
        / f"{parent_identity}_{workspace_name}_prev.json"
    )
```

**核心原则：路径命名规则必须是确定性的，不能用随机 token。**

### 6.4 委托记录文件要原子操作

两个 agent 可能并发写同一个委托记录文件：

```python
def on_delegation(self, task: str, result: str, **kwargs) -> None:
    record_path = self._get_delegation_record_path()
    
    # ✓ 原子追加：用 tempfile + rename
    tmp = record_path.with_suffix(".tmp")
    existing = json.load(record_path) if record_path.exists() else {"records": []}
    existing["records"].append({
        "task": task, 
        "result": result, 
        "timestamp": time.time(),
        **kwargs
    })
    json.dump(existing, tmp)
    tmp.rename(record_path)
```

### 6.5 子 agent（`skip_memory=True`）也能读快照

子 agent 初始化时 `skip_memory=True`，没有自己的 provider 实例。但它通过 `ephemeral_system_prompt`（context 参数）拿到父的快照内容，**子 agent 用 `read_file` 工具直接读取**。

因此快照内容格式必须**结构化且人类可读**，方便子 agent 解析：

```json
{
  "session_id": "20260502_080634",
  "timestamp": 1746163590,
  "workspace": "backend-api",
  "task_context": "正在开发用户认证模块，完成了 JWT 签名算法，下一步需要实现 RefreshToken 轮转",
  "delegation_chain": [
    {"task": "实现 JWT 签名", "result": "完成", "agent": "agent-1"}
  ]
}
```

### 6.6 写 Provider 时的 Checklist

| 检查项 | 要求 |
|--------|------|
| 快照路径命名 | 只能用 `agent_identity + workspace_name`，不能用随机 token |
| `initialize()` | 必须从 kwargs 提取并保存 `agent_identity`、`workspace_name`、`parent_session_id` |
| 任务转交 | 快照路径规则要让子 agent 可推导（确定性命名） |
| 文件写入 | 委托记录等并发写入场景用原子操作（tempfile + rename） |
| 快照格式 | JSON 结构化，task_context 字段要人类可读，方便子 agent 解析 |
| workspace 切换 | 新 workspace 启动时，读取的是上一个 session 的快照，不是当前 workspace 的 |

---

## 八、技术风险（独立 Workspace 场景）

| 风险 | 等级 | 应对 |
|------|------|------|
| `messages` 字段结构在大版本升级可能变化 | 低 | 快照存完整原始 JSON，不过度依赖字段名 |
| 子 agent `skip_memory=True` 意味着子 agent 无 provider | 中 | 通过 `ephemeral_system_prompt` 注入 context 解法已验证 |
| 快照文件增大导致 `system_prompt_block()` 超长 | 低 | 实施内容截断/摘要策略，控制在 4000 tokens 以内 |
| 多个独立 agent 场景下快照路径冲突 | 低 | 使用 `agent_identity + workspace_name` 双层隔离（确定性命名，禁止随机 token） |
| 子 agent 无法反推父 agent 快照路径 | 中 | 快照路径命名规则必须跨 agent 可推导，见 6.1/6.3 |
| 并发写委托记录文件导致数据损坏 | 低 | 原子操作（tempfile + rename） |

---

## 九、相关源码索引

| 文件 | 关键行 | 用途 |
|------|--------|------|
| `agent/memory_provider.py` | L153 `on_session_end` | 钩子定义 |
| `agent/memory_manager.py` | L285-294 `on_session_end` | 统一调用所有 provider |
| `run_agent.py` | L3749 `shutdown_memory_provider` | 触发 `on_session_end` |
| `run_agent.py` | L1442-1486 | provider 激活逻辑 |
| `plugins/memory/__init__.py` | L159 `load_memory_provider` | plugin 加载入口 |
| `tools/delegate_tool.py` | L90-110 `_build_child_system_prompt` | 子 agent 系统提示词构建 |
| `tools/delegate_tool.py` | L390 `ephemeral_system_prompt` | 子 agent 接收 context |
| `tools/delegate_tool.py` | L394 `skip_memory=True` | 子 agent 禁用 memory provider |

---

*文档版本：v1 | 调研：如音 | 待归档：思月*
