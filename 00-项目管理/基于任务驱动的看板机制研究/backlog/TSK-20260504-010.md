---
id: TSK-20260504-010
title: "[P2] 实时推送：WebSocket 任务状态同步"
status: archived
priority: P2
assignee: "xingruyin"
updated: 2026-05-07T02:13:38+08:00
---



















## 状态变更

**❌ 已废弃 — 原生 v0.12 已提供等效机制**

原生 `kanban_notify_subs` 表 + gateway notifier 实现事件订阅推送：

```sql
CREATE TABLE kanban_notify_subs (
    task_id       TEXT NOT NULL,
    platform      TEXT NOT NULL,
    chat_id       TEXT NOT NULL,
    thread_id     TEXT NOT NULL DEFAULT '',
    user_id       TEXT,
    created_at    INTEGER NOT NULL,
    last_event_id INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (task_id, platform, chat_id, thread_id)
);
```

gateway notifier watcher 监听 `task_events`，推送 `completed` / `blocked` / `spawn_auto_blocked` 到订阅者。

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin

## 快照记录

- **** `archived`
  - 摘要：已废弃，标记为 obsolete
  - 阻塞：(无)
  - 下步：(无)
  - Agent：xingruyin
