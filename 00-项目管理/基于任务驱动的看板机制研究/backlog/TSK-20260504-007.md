---

id: TSK-20260504-007
title: "[P1] Obsidian 同步层改造"
status: backlog
priority: P1
assignee: "xingruyin"
mission_id: kanban-phase2
created: 2026-05-04T00:00:00+08:00
updated: 2026-05-04T00:00:00+08:00

---

## 任务目标

改造 `upsert_from_obsidian()`，将 DB 连接从自建 tasks.db 改为原生 `kanban_db.py`，并加入状态映射层。

## 背景变更

原方案写入自建 `~/.hermes/kanban/tasks.db`，现已废弃。

整合方案要求统一到原生 `~/.hermes/kanban.db`，因此同步层需要改造。

## 改造点

### 1. DB 连接改造

```python
# 旧
conn = sqlite3.connect(str(_get_db_path()), timeout=10)

# 新
from hermes_cli import kanban_db as kb
conn = kb.connect()
```

### 2. 状态映射

```python
# 同步前将 Obsidian frontmatter 状态映射为原生状态
native_status = STATUS_MAP_TO_NATIVE.get(frontmatter_status, frontmatter_status)
```

### 3. mission_id 写入

```python
# frontmatter 中的 mission_id 映射到原生 tasks 表的 tenant 字段
# via kb.create_task(conn, ..., tenant=mission_id, ...)
```

## 同步规则（不变）

### SQLite → Obsidian（看板变更时）

| SQLite 字段 | Obsidian frontmatter |
|------------|---------------------|
| `status`（原生） | `status`（需映射回自建状态） |
| `priority` | `priority` |
| `assignee` | `assignee` |
| `updated_at` | `updated` |

### Obsidian → SQLite（人类编辑时）

| Obsidian frontmatter | SQLite 字段 |
|---------------------|------------|
| `status` | `status`（映射为原生状态） |
| `priority` | `priority` |
| `assignee` | `assignee` |
| `mission_id` | `tenant`（mission_id） |

## 验收标准

- [ ] `upsert_from_obsidian()` 写入原生 kanban.db
- [ ] 状态字段正确映射
- [ ] mission_id 正确写入 tenant 字段

## 依赖

- 🔴 TSK-004（迁移到原生 kanban.db）完成前无法测试

## 检查点

- [2026-05-04] 规划完成，等待 TSK-004 完成
