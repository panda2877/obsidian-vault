# 基于任务驱动的看板机制实施方案

> 本文档为技术实施主文档，修订时间：2026-05-04
> 研究目标：结合记忆快照机制与看板思路，设计一套适合多 Hermes profile 协作的长期记忆与任务协同方案。
> 策略：不做迁移，做嫁接 — 保留自建逻辑，复用原生实现

---

## 一、现状与问题

### 1.1 两套 DB 并存

| | 自建方案（原有） | 原生 v0.12 |
|--|---------|------------|
| DB 路径 | `~/.hermes/kanban/tasks.db` | `~/.hermes/kanban.db` |
| 状态机 | backlog/inbox/assigned/in_progress/review/done | triage/todo/ready/running/blocked/done/archived |
| 并发安全 | 无（connect→execute→close，无事务） | WAL + BEGIN IMMEDIATE |
| 任务历史 | ❌ 无 | ✅ task_runs（每次尝试完整记录） |
| 依赖链 | ❌ 无 | ✅ task_links |
| 上下文聚合 | ❌ 无 | ✅ build_worker_context |
| claim/心跳 | ❌ 无 | ✅ claim_lock + 15min TTL + heartbeat |
| 事件流 | ❌ 无 | ✅ task_events |
| agent 注册 | ✅ 有 | ❌ 无 |
| mission 维度 | ✅ 有 | ❌ 无（用 tenant 代替） |
| workspace | ❌ 无 | ✅ scratch/worktree/dir |

**核心问题**：两套 DB 完全隔离，状态分裂，多 agent 协同失效。

### 1.2 两种方案的核心差异

| 维度 | 快照（Snapshot） | 看板（Kanban） |
|------|-------------------|----------------|
| 存储介质 | JSON 文件 | SQLite |
| 状态粒度 | 整个会话 | 单个任务 |
| 多 agent 感知 | ❌ | ✅ missionId 隔离 |
| 上下文完整性 | ✅ 被动全量保存 | ⚠️ 依赖主动写入 |
| 人工干预 | 需要透传 | 直接改看板 |
| 失败恢复 | ✅ 上下文完整恢复 | ⚠️ 需要 checkpoint 机制 |

### 1.3 关键洞察

看板方案在「上下文不丢失」上弱于快照。如果 agent 在任务中途崩溃（且没有主动写 checkpoint），看板只知道「任务在 running」，但之前做了哪些决策、试了什么方法、哪里卡住——全部丢失。

快照的保障是「被动」的：进程退出前自动捕获整个消息状态。
看板的保障是「主动」的：依赖 agent 在任务阶段变更时主动写入上下文。

---

## 二、设计目标

1. **保留快照的上下文保障**：不丢失任何工作进度
2. **引入看板的多 agent 协同能力**：任务状态可见、可追踪、可干预
3. **设计 checkpoint 机制**：解决看板在失败恢复上的弱点
4. **统一 DB 路径到原生**：`~/.hermes/kanban.db`，不复用自建 DB
5. **复用原生实现**：并发安全、claim 锁、task_runs、build_worker_context

---

## 三、整合原则

**不做迁移，做嫁接。**

```
自建逻辑（保留）
├── agent 注册
├── mission 维度
├── 状态机映射（backlog→done ↔ 原生状态）
├── Obsidian frontmatter 同步
└── Checkpoint 表 + Hook 触发逻辑

嫁接原生（复用）
├── SQLite WAL + BEGIN IMMEDIATE（并发安全）
├── task_runs（尝试历史）
├── task_links（依赖链）
├── build_worker_context（上下文聚合）
├── claim_lock / heartbeat（分布式锁）
├── task_events（事件流）
└── hermes kanban CLI + kanban_tools 工具链
```

---

## 四、DB 路径统一

**决策**：弃用自建 `~/.hermes/kanban/tasks.db`，统一到原生 `~/.hermes/kanban.db`。

统一后：
- 所有 agent（不论 profile）共用同一份 DB
- 原生 CLI 和工具链直接可见
- 自建 Hook 和 Checkpoint 逻辑无需修改路径

---

## 五、Schema 变更

### 5.1 新增列（通过原生迁移机制）

在 `kanban_db.py` 的 `_migrate_add_optional_columns()` 中追加：

```python
if "mission_id" not in cols:
    conn.execute("ALTER TABLE tasks ADD COLUMN mission_id TEXT")
```

### 5.2 Checkpoint 表（自建，保留）

原生没有 Checkpoint 概念，这是自建方案的核心保障机制，保留在原生 kanban.db 中：

```sql
CREATE TABLE IF NOT EXISTS checkpoints (
    id          TEXT PRIMARY KEY,
    task_id     TEXT NOT NULL,
    phase       TEXT NOT NULL,
    summary     TEXT,
    blockers    TEXT,
    next_steps  TEXT,
    agent_id    TEXT,
    created_at  TEXT NOT NULL,
    FOREIGN KEY (task_id) REFERENCES tasks(id)
);
CREATE INDEX IF NOT EXISTS idx_checkpoint_task ON checkpoints(task_id);
```

### 5.3 Agent Registry 表（自建，保留）

原生靠 profile 环境变量，没有显式 agent 注册表。自建方案需要保留：

```sql
CREATE TABLE IF NOT EXISTS agent_registry (
    id            TEXT PRIMARY KEY,
    profile       TEXT NOT NULL UNIQUE,
    name          TEXT NOT NULL,
    role          TEXT NOT NULL DEFAULT 'generalist',
    capabilities  TEXT NOT NULL DEFAULT '[]',
    status        TEXT NOT NULL DEFAULT 'idle',
    registered_at TEXT NOT NULL
);
```

---

## 六、状态机映射

自建状态与原生状态的对应关系：

| 自建 | 原生 | 说明 |
|------|------|------|
| backlog | triage | 等进入看板队列 |
| inbox | todo | 人工整理阶段 |
| assigned | ready | 已分派，待认领 |
| in_progress | running | 执行中 |
| review | running | review 复用 running，语义由标记区分 |
| done | done | 完成 |
| — | blocked | 新增（人工干预入口） |
| — | archived | 新增（归档） |

```python
STATUS_MAP_TO_NATIVE = {
    "backlog": "triage",
    "inbox": "todo",
    "assigned": "ready",
    "in_progress": "running",
    "review": "running",
    "done": "done",
}

STATUS_MAP_FROM_NATIVE = {
    "triage": "backlog",
    "todo": "inbox",
    "ready": "assigned",
    "running": "in_progress",
    "blocked": "in_progress",
    "done": "done",
    "archived": "done",
}
```

---

## 七、tasks_store.py 重构

### 7.1 改造思路

删掉所有自建 DB 底层操作，直接调用原生 `kanban_db.py`：
- 连接：`kanban_db.connect()`
- 事务：`kanban_db.write_txn()`
- 任务操作：调用原生 `create_task` / 直接执行 UPDATE 等

**保留**：
- Obsidian frontmatter 同步（`upsert_from_obsidian`）
- agent 注册（`register_agent` / `update_agent_status`）
- mission 维度映射
- 状态映射层

### 7.2 具体改造点

#### 连接层

```python
# 旧
conn = sqlite3.connect(str(_get_db_path()), timeout=10)

# 新
from hermes_cli import kanban_db as kb
conn = kb.connect()
```

#### 事务层

```python
# 旧
conn.execute(...)
conn.commit()

# 新
with kb.write_txn(conn):
    conn.execute(...)
```

#### create_task

```python
# 新：调用原生
kb.create_task(
    conn,
    title=title,
    body=description,
    assignee=assignee,
    tenant=mission_id,  # mission_id 映射到原生 tenant 字段
    triage=True,  # backlog 映射到 triage
    ...
)
```

#### update_task_status

```python
# 新：先映射状态，再调用原生
native_status = STATUS_MAP_TO_NATIVE.get(status, status)
with kb.write_txn(conn):
    conn.execute(
        "UPDATE tasks SET status = ?, updated_at = ? WHERE id = ?",
        (native_status, now, task_id)
    )
```

#### agent 注册

保留独立 `agent_registry` 表，不借用原生任何机制。

#### Checkpoint 操作

独立保留，路径指向原生 kanban.db 里的 Checkpoint 表。

---

## 八、Checkpoint 机制

### 8.1 触发时机

| 事件 | 触发 |
|------|------|
| `agent:start` | 读取最新 checkpoint，还原上下文 |
| `agent:end` | 写入 checkpoint（从 `finished=true` 判断） |
| 任务状态变更 | 通过 Hook 系统触发 |

### 8.2 Checkpoint 内容

```python
{
    "phase": "implementing",   # 或 "wrapping"
    "findings": [...],        # 从 extract_task_context 映射
    "blockers": [...],        # 从 missing_info 映射
    "next_steps": [...]
}
```

### 8.3 与原生 task_runs 的区别

| | Checkpoint | 原生 task_runs |
|--|-----------|----------------|
| 粒度 | 阶段级（implementing/wrapping） | 尝试级（一次 running 即一条记录） |
| 内容 | findings/blockers/next_steps | summary/metadata/error |
| 触发 | agent:end + phase_change Hook | claim → complete/block |
| 用途 | 跨阶段上下文恢复 | 跨尝试历史追溯 |

两者互补，不冲突。

---

## 九、Hook 逻辑

自建 Hook 路径：`~/.hermes/hooks/kanban-checkpoint/`

```
kanban-checkpoint/
├── HOOK.yaml        # 声明事件监听
└── handler.py       # checkpoint 写入逻辑
```

改动点：
- DB 路径改为原生 `~/.hermes/kanban.db`
- 读取/写入 Checkpoint：从原生 kanban.db 的 checkpoints 表
- 任务状态操作：调用原生 `kanban_db` 函数

---

## 十、Obsidian 同步

`upsert_from_obsidian()` 逻辑保持不变，只需改造底层 DB 调用为原生 `kanban_db.connect()`。

同步后任务写入原生 kanban.db，状态通过映射层转换。

---

## 十一、实施步骤

### Step 1：修改原生 kanban_db.py 迁移

文件：`hermes_cli/kanban_db.py`

在 `_migrate_add_optional_columns()` 中追加：

```python
if "mission_id" not in cols:
    conn.execute("ALTER TABLE tasks ADD COLUMN mission_id TEXT")
```

### Step 2：创建 Checkpoint + Agent 表

```python
from hermes_cli.kanban_db import connect

def init_custom_tables():
    conn = connect()
    conn.execute("""
        CREATE TABLE IF NOT EXISTS checkpoints (
            id TEXT PRIMARY KEY,
            task_id TEXT NOT NULL,
            phase TEXT NOT NULL,
            summary TEXT,
            blockers TEXT,
            next_steps TEXT,
            agent_id TEXT,
            created_at TEXT NOT NULL
        )
    """)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS agent_registry (
            id TEXT PRIMARY KEY,
            profile TEXT NOT NULL UNIQUE,
            name TEXT NOT NULL,
            role TEXT NOT NULL DEFAULT 'generalist',
            capabilities TEXT NOT NULL DEFAULT '[]',
            status TEXT NOT NULL DEFAULT 'idle',
            registered_at TEXT NOT NULL
        )
    """)
    conn.close()
```

### Step 3：改造 tasks_store.py

1. 删除所有 `sqlite3.connect` 调用，替换为 `kb.connect()`
2. 删除 `_ensure_db()` 函数，替换为 `init_custom_tables()`
3. 所有任务操作调用原生 `kanban_db` 函数
4. 加状态映射层
5. Checkpoint 操作独立保留

### Step 4：验证

```bash
# 确认 kanban.db 存在且包含新列
sqlite3 ~/.hermes/kanban.db ".schema tasks"

# 确认 Checkpoint 和 Agent 表存在
sqlite3 ~/.hermes/kanban.db ".tables"

# 测试任务创建
hermes kanban create "测试任务" --assignee xingruyin

# 测试 Hook 触发
# 向任意 agent 发消息，观察 checkpoint 写入
```

---

## 十二、风险与备选

### 12.1 mission_id vs tenant

原生用 `tenant` 字段做多租户隔离。自建方案用 `mission_id`。映射关系：
- `task.mission_id` → 写入原生 `task.tenant`
- 查询时按 `tenant` 过滤 = 按 `mission_id` 过滤

### 12.2 自建状态丢失

状态映射到原生后，原生状态（如 `blocked`）无法映射回自建状态。**这是可接受的**：新状态是原生的增强，自建方案向原生靠拢。

### 12.3 并发安全

直接复用原生 WAL + BEGIN IMMEDIATE，无需自建锁机制。

### 12.4 SIGKILL 盲区

atexit 无法覆盖 SIGKILL/OOM，这是 Snapshot 的固有限制，看板方案同样存在。
备选：定期写入 checkpoint（如每 N 轮对话后），降低单次崩溃的丢失量。

---

## 十三、文件变更清单

| 文件 | 操作 |
|------|------|
| `hermes_cli/kanban_db.py` | 修改迁移函数，追加 mission_id 列 |
| `plugins/memory/snapshot/tasks_store.py` | 重构，调用原生 DB 层 |
| `~/.hermes/hooks/kanban-checkpoint/handler.py` | DB 路径改为原生路径 |

---

## 十四、相关文件路径

**Hermes Agent 后端：**
- Snapshot 插件：`plugins/memory/snapshot/`
  - `provider.py` — SnapshotMemoryProvider（含 write_checkpoint 公共 API）
  - `checkpoint_store.py` — SQLite checkpoint 存储
  - `context_extractor.py` — 任务上下文正则解析
  - `snapshot_store.py` — JSON 快照文件存储
- Hook 系统：`gateway/hooks.py`
- Kanban Checkpoint Hook：`~/.hermes/hooks/kanban-checkpoint/`（自建）

**参考文档：**
- [[看板机制与原生v0.12整合方案]] — 详细整合实施文档
- [[看板开发任务]] — 任务卡片与进度追踪
