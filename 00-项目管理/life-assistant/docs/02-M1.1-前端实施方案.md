# M1.1 实施方案：前端新增"生活"Tab及路由

**里程碑**: M1 - 融合基建  
**任务**: M1.1 - 前端：新增生活Tab和基础页面结构  
**负责人**: ziling  
**状态**: 📋 待执行  

---

## 📌 目标

在 Hermes Dashboard 中新增"生活"Tab，包含记账和待办两个核心页面的基础框架。

---

## 📁 文件变更清单

### 1. 修改 `pages.json`（路由配置）

**文件**: `public/hermes-dashboard/src/pages.json`

**变更内容**:

```json
// 在 pages 数组中添加
{
  "path": "pages/life/index",
  "style": {
    "navigationBarTitleText": "生活助手",
    "navigationStyle": "custom",
    "backgroundColor": "#08090a"
  }
},
{
  "path": "pages/life/finance",
  "style": {
    "navigationBarTitleText": "记账",
    "navigationStyle": "custom",
    "backgroundColor": "#08090a"
  }
},
{
  "path": "pages/life/todo",
  "style": {
    "navigationBarTitleText": "待办",
    "navigationStyle": "custom",
    "backgroundColor": "#08090a"
  }
}

// 在 tabBar.list 中添加
{
  "pagePath": "pages/life/index",
  "text": "生活",
  "iconPath": "static/icons/life.svg",
  "selectedIconPath": "static/icons/life-active.svg"
}
```

### 2. 创建页面文件

**目录结构**:
```
src/pages/life/
├── index.vue    # 生活助手首页（入口页）
├── finance.vue  # 记账页面
└── todo.vue     # 待办页面
```

**index.vue**（首页）:
```vue
<template>
  <view class="life-home">
    <view class="quick-actions">
      <view class="action-card" @click="navigateTo('finance')">
        <text class="icon">💰</text>
        <text class="title">记账</text>
        <text class="desc">记录收支</text>
      </view>
      <view class="action-card" @click="navigateTo('todo')">
        <text class="icon">✅</text>
        <text class="title">待办</text>
        <text class="desc">任务管理</text>
      </view>
    </view>
    <view class="summary">
      <!-- 待后续填充 -->
    </view>
  </view>
</template>

<script setup lang="ts">
const navigateTo = (path: string) => {
  uni.navigateTo({ url: `/pages/life/${path}` })
}
</script>

<style lang="scss" scoped>
.life-home {
  padding: 20rpx;
  .quick-actions {
    display: flex;
    gap: 20rpx;
    .action-card {
      flex: 1;
      background: #1a1c1f;
      border-radius: 16rpx;
      padding: 40rpx 20rpx;
      text-align: center;
      .icon { font-size: 60rpx; display: block; margin-bottom: 10rpx; }
      .title { font-size: 32rpx; font-weight: bold; color: #fff; }
      .desc { font-size: 24rpx; color: #888; margin-top: 10rpx; }
    }
  }
}
</style>
```

**finance.vue**（记账页）:
```vue
<template>
  <view class="finance-page">
    <view class="quick-record">
      <!-- 快速记账表单 -->
    </view>
    <view class="record-list">
      <!-- 记账列表 -->
    </view>
  </view>
</template>

<script setup lang="ts">
// 待实现
</script>
```

**todo.vue**（待办页）:
```vue
<template>
  <view class="todo-page">
    <view class="add-task">
      <!-- 添加任务 -->
    </view>
    <view class="task-list">
      <!-- 任务列表 -->
    </view>
  </view>
</template>

<script setup lang="ts">
// 待实现
</script>
```

### 3. 创建图标文件

**文件**: `static/icons/life.svg` 和 `static/icons/life-active.svg`

使用与现有图标相同的风格（暗色主题，选中态高亮）。

---

## 📝 执行步骤

| 步骤 | 操作 | 预计耗时 |
|------|------|----------|
| 1 | 修改 `pages.json` 添加路由配置 | 5min |
| 2 | 修改 `pages.json` 添加 TabBar 配置 | 5min |
| 3 | 创建 `src/pages/life/` 目录 | 1min |
| 4 | 创建 `index.vue` 首页 | 15min |
| 5 | 创建 `finance.vue` 记账页（骨架） | 10min |
| 6 | 创建 `todo.vue` 待办页（骨架） | 10min |
| 7 | 创建/准备图标文件 | 5min |
| 8 | 测试Tab切换和页面跳转 | 10min |

**总计**: 约 60min

---

## ✅ 验收标准

- [ ] "生活"Tab 在底部导航栏正常显示
- [ ] 点击"生活"Tab 可进入生活助手首页
- [ ] 首页显示"记账"和"待办"入口卡片
- [ ] 点击卡片可跳转到对应页面
- [ ] 页面样式与现有暗色主题一致
- [ ] H5端和App端均可正常访问

---

## 🚧 依赖与注意

- **依赖**: 无（独立任务）
- **注意**: TabBar 最多支持5个Tab，现有4个 + 新增1个 = 5个，刚好满额
- **图标**: 可使用 emoji 临时替代，后续替换为SVG
