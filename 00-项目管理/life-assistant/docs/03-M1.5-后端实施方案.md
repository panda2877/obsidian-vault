# M1.5 实施方案：后端新增生活助手路由骨架

**里程碑**: M1 - 融合基建  
**任务**: M1.5 - 后端：新增生活助手路由骨架  
**负责人**: ziling  
**状态**: 📋 待执行  

---

## 📌 目标

在现有 Express BFF 中新增 `/api/life/*` 路由组，实现生活助手核心功能的API骨架。

---

## 📁 文件变更清单

### 1. 创建路由文件 `backend/routes/life.js`

```javascript
/**
 * 生活助手 API 路由
 * 路径: /api/life/*
 */

const express = require('express')
const router = express.Router()
const auth = require('../middleware/auth') // 复用现有认证中间件

// 所有路由需要认证
router.use(auth)

// ── 记账相关 ─────────────────────────────────────────────────────────────────

/**
 * POST /api/life/finance
 * 创建记账记录
 */
router.post('/finance', async (req, res) => {
  try {
    const { type, amount, category_id, note, record_date } = req.body
    const user_id = req.user.id

    // TODO: 实现数据库写入
    // INSERT INTO finance_records (...) VALUES (...)

    res.json({
      success: true,
      data: { id: 'uuid-placeholder', user_id, type, amount, category_id, note, record_date }
    })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * GET /api/life/finance
 * 获取记账列表（支持分页、筛选）
 */
router.get('/finance', async (req, res) => {
  try {
    const user_id = req.user.id
    const { page = 1, limit = 20, type, category_id, start_date, end_date } = req.query

    // TODO: 实现数据库查询
    // SELECT * FROM finance_records WHERE user_id = ? ...

    res.json({
      success: true,
      data: [],
      pagination: { page: +page, limit: +limit, total: 0 }
    })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * PUT /api/life/finance/:id
 * 更新记账记录
 */
router.put('/finance/:id', async (req, res) => {
  try {
    const user_id = req.user.id
    const { id } = req.params
    const { type, amount, category_id, note, record_date } = req.body

    // TODO: 实现数据库更新
    // UPDATE finance_records SET ... WHERE id = ? AND user_id = ?

    res.json({ success: true })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * DELETE /api/life/finance/:id
 * 删除记账记录
 */
router.delete('/finance/:id', async (req, res) => {
  try {
    const user_id = req.user.id
    const { id } = req.params

    // TODO: 实现数据库删除
    // DELETE FROM finance_records WHERE id = ? AND user_id = ?

    res.json({ success: true })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

// ── 分类相关 ─────────────────────────────────────────────────────────────────

/**
 * GET /api/life/categories
 * 获取分类列表（含系统预设 + 用户自定义）
 */
router.get('/categories', async (req, res) => {
  try {
    const user_id = req.user.id

    // TODO: 实现数据库查询
    // SELECT * FROM finance_categories WHERE user_id = ? OR is_system = true

    res.json({
      success: true,
      data: [
        // 系统预设分类示例
        { id: 'sys-001', name: '餐饮', type: 'out', icon: '🍔', is_system: true },
        { id: 'sys-002', name: '交通', type: 'out', icon: '🚗', is_system: true },
        { id: 'sys-003', name: '购物', type: 'out', icon: '🛒', is_system: true },
        { id: 'sys-004', name: '工资', type: 'in', icon: '💰', is_system: true },
      ]
    })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

// ── 待办相关 ─────────────────────────────────────────────────────────────────

/**
 * POST /api/life/todo
 * 创建待办任务
 */
router.post('/todo', async (req, res) => {
  try {
    const { title, description, category, priority, due_date } = req.body
    const user_id = req.user.id

    // TODO: 实现数据库写入
    // INSERT INTO todo_tasks (...) VALUES (...)

    res.json({
      success: true,
      data: { id: 'uuid-placeholder', user_id, title, description, category, priority, status: 'todo', due_date }
    })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * GET /api/life/todo
 * 获取待办列表（支持按状态、分类筛选）
 */
router.get('/todo', async (req, res) => {
  try {
    const user_id = req.user.id
    const { status, category } = req.query

    // TODO: 实现数据库查询
    // SELECT * FROM todo_tasks WHERE user_id = ? ...

    res.json({
      success: true,
      data: []
    })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * PUT /api/life/todo/:id
 * 更新待办任务
 */
router.put('/todo/:id', async (req, res) => {
  try {
    const user_id = req.user.id
    const { id } = req.params
    const { title, description, category, priority, status, due_date } = req.body

    // TODO: 实现数据库更新
    // UPDATE todo_tasks SET ... WHERE id = ? AND user_id = ?

    res.json({ success: true })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

/**
 * DELETE /api/life/todo/:id
 * 删除待办任务
 */
router.delete('/todo/:id', async (req, res) => {
  try {
    const user_id = req.user.id
    const { id } = req.params

    // TODO: 实现数据库删除
    // DELETE FROM todo_tasks WHERE id = ? AND user_id = ?

    res.json({ success: true })
  } catch (err) {
    res.status(500).json({ error: err.message })
  }
})

module.exports = router
```

### 2. 修改 `backend/server.js` 挂载路由

```javascript
// 在路由挂载部分添加
const lifeRouter = require('./routes/life')
app.use('/api/life', lifeRouter)
```

### 3. 检查认证中间件

确认 `backend/middleware/auth.js` 存在并正常工作（复用现有JWT认证）。

---

## 📝 执行步骤

| 步骤 | 操作 | 预计耗时 |
|------|------|----------|
| 1 | 检查现有认证中间件 `middleware/auth.js` | 5min |
| 2 | 创建 `backend/routes/life.js` | 30min |
| 3 | 修改 `backend/server.js` 挂载路由 | 5min |
| 4 | 启动后端服务，测试API路由 | 10min |
| 5 | 使用 curl/Postman 验证路由骨架 | 10min |

**总计**: 约 60min

---

## ✅ 验收标准

- [ ] `/api/life/*` 路由正常挂载
- [ ] 所有路由需要 JWT Token 认证
- [ ] 未认证请求返回 401
- [ ] 认证后返回占位数据（骨架验证）
- [ ] 路由与现有路由无冲突

---

## 🚧 依赖与注意

- **依赖**: 现有 JWT 认证中间件 (`middleware/auth.js`)
- **数据库**: 路由骨架暂不连接数据库（占位实现）
- **注意**: 确保 `user_id` 从 Token 中正确提取并用于数据隔离
