# 多Agent记忆机制优化方案

> 创建时间：2026-04-28
> 版本：v1.1
> 负责人：如音
> 用途：优化银月+姐妹体系的记忆机制，建立三层记忆架构和心跳维护流程
>
> 变更记录：
> - v1.1：明确启动定义（session粒度）、压缩触发条件与兜底逻辑、巡检增加skill提炼检查、精简模板删除"当前任务"和"变更记录"、新增文件结构总览

---

## 一、现状与问题

### 1.1 现有记忆机制

| 层级 | 存储位置 | 容量 | 加载方式 | 利用率 |
|------|---------|------|---------|-------|
| 短期（会话） | LLM context window | Token 上限 | 自动 | ✅ 100% |
| 中期（日志） | `memories/` 目录 | 无限制 | 需主动读取 | ❌ 0% |
| 长期（永久） | MEMORY.md + USER.md | 2200+1375 字符 | 每次启动自动 | ⚠️ 有内容无维护 |

### 1.2 核心问题

1. **无中期层**：跨天任务无法接续，上下文全靠重新描述
2. **MEMORY.md 只增不减**：长期积累超过 2200 字符上限后会丢失早期内容
3. **无巡检机制**：失效规则不会被清理，MEMORY.md 越来越臃肿
4. **姐妹协作信息孤岛**：各姐妹独立记忆，对同一项目的认知可能不一致

---

## 二、三层记忆架构设计

### 2.1 层级定义

```
┌─────────────────────────────────────────────────────────────┐
│  第一层：会话级（自动）                                      │
│  当前对话的上下文，由 LLM 原生管理                            │
│  - 会话结束后即销毁                                          │
└─────────────────────────────────────────────────────────────┘
                          ↓ 主动归档
┌─────────────────────────────────────────────────────────────┐
│  第二层：中期记忆（日志）                                     │
│  路径：~/.hermes/profiles/{name}/memories/YYYY-MM-DD.md     │
│  - 每日自动生成                                             │
│  - 包含：任务进度、关键决策、遗留问题、下一步行动              │
│  - 下次启动时读取，自动接续                                   │
└─────────────────────────────────────────────────────────────┘
                          ↓ 定期提炼
┌─────────────────────────────────────────────────────────────┐
│  第三层：长期记忆（永久）                                     │
│  路径：~/.hermes/profiles/{name}/MEMORY.md + USER.md        │
│  - MEMORY.md：技术规范、分工规则、工具路径                    │
│  - USER.md：老大偏好、沟通风格、禁止事项                      │
│  - 字符限额：2200 + 1375                                     │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 各姐妹的记忆分工

| 姐妹 | MEMORY.md 重点 | USER.md 重点 |
|------|---------------|-------------|
| 银月 | 项目全局状态、当前任务进度、协作路由规则 | 老大沟通偏好 |
| 如音 | 技术规范、服务器环境、已知 Bug、工具配置 | 分工边界、技术偏好 |
| 思月 | 文档归档状态、知识库结构、归档流程规范 | 老大文档偏好 |
| 紫灵 | 需求创意规范、产品设计知识、创意标准 | 老大创意偏好 |

### 2.3 文件结构总览

| 文件/目录 | 位置 | 作用 |
|-----------|------|------|
| SOUL.md | `~/.hermes/profiles/{name}/SOUL.md` | 角色定义、性格话术、协作边界 |
| MEMORY.md | `~/.hermes/profiles/{name}/MEMORY.md` | 长期记忆：技术规范、分工规则、环境信息 |
| USER.md | `~/.hermes/profiles/{name}/USER.md` | 老大偏好、沟通风格、禁止事项 |
| memories/ | `~/.hermes/profiles/{name}/memories/YYYY-MM-DD.md` | 中期记忆：每日日志 |
| skills/ | `~/.hermes/skills/` | 可复用技能库（各姐妹共享） |
| CHANGELOG.md | `~/.hermes/profiles/{name}/CHANGELOG.md` | 记忆变更记录（新建） |
| 文档归档 | `~/obsidian-vault/` | 姐妹们的工作文档（思月维护） |

---

## 三、中期记忆层实施

### 3.1 日志文件格式

路径：`~/.hermes/profiles/{name}/memories/YYYY-MM-DD.md`

```markdown
# {YYYY-MM-DD} 工作日志 — {姐妹名}

## 今日完成
-

## 进行中
-

## 待处理
-

## 关键决策
-

## 遗留问题
-
```

### 3.2 启动时自动读取机制

**说明**："启动"指**新建 session**（即银月发起新对话），而非重启网关。每次新建 session 时加载完 SOUL/MEMORY 后，自动进入读取流程。

**问题**：各姐妹每次启动都是空白状态，不知道昨天做了什么。

**解决**：在 SOUL.md 或 MEMORY.md 中固化启动流程，每次新建 session 后先读取近期日志。

```bash
# 在 MEMORY.md 开头固化：
## 启动流程
每次启动时：
1. 读取 memories/ 目录下近 3 天的日志文件
2. 从日志中提取「进行中」和「待处理」事项
3. 若有未完成的上文任务，主动询问老大是否接续
```

### 3.3 任务交接时写入日志

当姐妹之间需要交接任务时，交出方必须写入日志：

```markdown
## 任务交接：{任务名} → {接替姐妹}
- 当前进度：...
- 关键信息：...
- 注意事项：...
- 接替者需要：...
```

---

## 四、心跳巡检机制（长期记忆维护）

### 4.1 问题

MEMORY.md 有 2200 字符上限，长期只增不减会导致：
- 早期重要规则被挤出
- 失效规则占用空间
- 记忆质量下降

### 4.2 巡检 Cron 任务

为每个姐妹创建每日巡检 Cron：

```bash
# 如音的巡检 Cron
hermes cron create "0 21 * * *" \
  --name "如音-记忆巡检" \
  --profile xingruyin \
  --message "执行记忆巡检任务：
1. 读取 memories/ 近 3 天日志，提炼新增的重要规则
2. 检查 MEMORY.md 当前字符数：
   - 若超过 1800 字符，执行压缩（清理失效记录、精简长表述）
   - 若不超过 1800 字符，本次跳过压缩
   - 若连续 3 次无需压缩，提示老大考虑将长规范迁移为 skill
3. 将本次巡检结果写入 memories/YYYY-MM-DD.md" \
  --deliver origin
```

**压缩触发条件**：MEMORY.md 实际字符数 > 1800
**"无法精简"时的处理**：在当天日志中记录"本次无需压缩"；若连续 3 次均无需压缩，主动提示老大将部分规范迁移至独立文件或提炼为 skill。

### 4.3 巡检检查清单

```
记忆巡检清单：
□ 读取 memories/ 近 3 天日志
□ 提取新出现的规范/决策/偏好
□ 更新 MEMORY.md（新增重要内容）
□ 清理失效的临时记录
□ 若 MEMORY.md > 1800 字符，执行精简压缩
□ 评估是否有长规范可提炼为独立 skill
□ 在当天 memories 日志中记录本次巡检结果
```

---

## 五、MEMORY.md 精简模板

当前如音的 MEMORY.md 约 1726 字符，接近上限。需按以下模板精简：

```markdown
# {姐妹名} - 记忆档案

## 老大信息
- 身份/位置/沟通偏好（精简，不超过 3 行）

## 分工边界
- 我的职责：...
- 其他姐妹职责：...

## 技术环境
- 关键路径/工具/规范（精简，不超过 5 行）

## 重要规范
- 本项目中经确认必须遵守的规则（精简，不超过 5 条）

---

*由心跳机制维护，自动更新*
```

---

## 六、实施计划

| 阶段 | 行动 | 说明 |
|------|------|------|
| P0 | 各姐妹优化自己的 MEMORY.md | 按第五章模板精简，当前均已接近/超过 1200 字符 |
| P1 | 补充启动时读取日志流程 | 在 MEMORY.md 开头固化，或在 SOUL.md 中描述 |
| P2 | 创建巡检 Cron | 每个姐妹一条每日 21:00 巡检 |
| P3 | 测试任务交接日志 | 指定一个场景验证交接流程 |
| P4 | 验证启动时日志接续 | 重启某姐妹，验证是否读取日志 |

---

## 七、关联文档

- [[多Agent化技术方案-20260427]] — 姐妹协作架构主文档
- [[记忆系统]] — Hermes 原生记忆机制说明
- [[Obsidian文档管理流程v1.0]] — 思月归档流程（记忆的一部分）
